package edu.onu.kennedy_map;

/**
 * Used to scale the bitmap and position it correctly
 */
public class TransMatrixID {
    static final short Width = 0, Height = 1, Scale = 2, xOffset = 3, yOffset = 4;
}
